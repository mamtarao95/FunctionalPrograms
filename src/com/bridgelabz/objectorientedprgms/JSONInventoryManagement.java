package com.bridgelabz.objectorientedprgms;

import java.io.File;
import java.io.FileReader;
import java.io.IOException;

import org.codehaus.jackson.JsonGenerationException;
import org.codehaus.jackson.map.JsonMappingException;
import org.codehaus.jackson.map.ObjectMapper;
import org.json.simple.JSONArray;
import org.json.simple.JSONObject;
import org.json.simple.parser.JSONParser;

public class JSONInventoryManagement {
	
	public static void main(String[] args) {
		int inventoryPrice=0;
		JSONInventory jsonInventory=new JSONInventory();
		
		    
		/*for (Object object2 : jsonInventory.inventoryarray) {
	        JSONObject inventory=(JSONObject)object2;
	        //System.out.println(inventory.toString());
	       
	        long weight=(long)inventory.get("Weight");
	        long price=(long)inventory.get("Price");
	        totalprice+=(weight*price);
	        }
	        System.out.println();
	        System.out.println("Price of "+inventorykeys+" is "+totalprice);
	    }   

		     JSONArray array = (JSONArray) jsonObject.get("stockList");
		     for (int i = 0; i <array.size(); i++) {
		         JSONObject obstock = (JSONObject) (array.get(i));
		         inventoryPrice += ((long) obstock.get("noOfShare")) * ((long) obstock.get("sharePrice"));
		         PER_STOCK_VALUE= ((long) obstock.get("noOfShare")) * ((long) obstock.get("sharePrice"));
		         System.out.println("The stockvalue of "+ (i+1)+" stock is "+ PER_STOCK_VALUE);
		     }
		     System.out.println("the total stock value is"+ TOTAL_STOCK);*/
		    
		 }
		
		
	}
	
	

